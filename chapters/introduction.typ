= Introduction

// Modern software projects and systems are becoming increasingly more complex.
// To keep this complexity in check, it has long been standard practice in software development to use so-called design patterns to 
// reason about systems on a higher level of abstraction. A short overview over the origins of the term and standard reference works are given.

// Metaprogramming is a powerful technique that software developers can use to express abstractions in code form and reduce verboseness and
// code repetition. One such programming technique is aspect-oriented programming (AOP), which aims to abstract concerns out of our code by 
// the application of aspects, which can in turn be automatically implemented in our user-code via code generation. We will give a short overview over options for such code generation frameworks in @approach

// This leaves us with the obvious question of how viable it is to represent various design patterns as aspects and implement them via code generation frameworks, which is the primary point of research this paper covers.

// == Software design patterns

// Wirklich nur kurze Erklärung und Einführung zu Softwarepatterns

// == Metaprogramming and aspect-oriented programming

// == Motivation and objectives

// Motivation der Arbeit im Sinne von: Implementierung von Patterns automatisieren, weg von Code hin zu High-Level concerns (Nenne auch Aspect Oriented Programming)

// 1. Automatisieren von Implementierungen von Patterns
// 2. Fehlerreduktion durch verifizierbar korrektem generiertem Code
// 3. Einfacheres Maintainen von Code durch autogenerieren (Beispiel: Entity Klasse, die Memento implementiert, ändert sich => spielt auch wieder mit Punkt 2 zusammen)